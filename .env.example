# Copy to `.env` for local scraping. NEVER commit `.env`.

# Each club gets its own pair. Add as GitHub Secrets for the cron action.
PC_SUNBURY_EMAIL=
PC_SUNBURY_PASS=

PC_ROEGREEN_EMAIL=
PC_ROEGREEN_PASS=

# Optional — Slack webhook to ping on scraper failure
SLACK_WEBHOOK_FAILURE=
